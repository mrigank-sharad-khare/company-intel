streamlit>=1.40
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.0
tldextract>=5.1
pandas>=2.2
reportlab>=4.2
plotly>=5.24
python-dotenv>=1.0

# LLM provider (install the one you use)
anthropic>=0.40
# openai>=1.50

# Web search provider (optional but strongly recommended — see README)
tavily-python>=0.5

# JS-heavy site fallback (optional). After installing, run:
#   playwright install chromium
playwright>=1.48
